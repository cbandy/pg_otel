receivers:
  pg_otel_logs:

processors:
  batch:

exporters:
  file: { path: /tmp/pg-otel.ndjson }

service:
  pipelines:
    logs:
      receivers: [pg_otel_logs]
      processors: [batch]
      exporters: [file]
